package AgentProvider.Implementation.Agents;

/**
 * The possible values agent settings that can be changed.
 */
public enum AgentSettingName {
    ALPHA,
    EPSILON,
    GAMMA,
    LAMBDA
}
